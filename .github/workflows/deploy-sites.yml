name: build-and-deploy

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  quartz:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Clone Quartz v4
        run: git clone --depth 1 --branch v4 https://github.com/jackyzha0/quartz.git /tmp/quartz

      - name: Copy notes into Quartz content
        run: |
          rsync -av --delete \
            "$GITHUB_WORKSPACE"/ \
            /tmp/quartz/content/ \
            --exclude '.git' \
            --exclude '.github' \
            --exclude '.obsidian'

      - name: Install Quartz deps
        working-directory: /tmp/quartz
        run: npm ci

      - name: Configure Quartz
        working-directory: /tmp/quartz
        run: |
          sed -i "s/pageTitle: .*/pageTitle: \"📝 Malinkang's Notes\",/" quartz.config.ts
          sed -i 's/footer: Component\.Footer.*/footer: Component.Footer({ links: { GitHub: "https:\/\/github.com\/malinkang", Twitter: "https:\/\/twitter.com\/malinkang" } }),/' quartz/components/Footer.tsx 2>/dev/null || true
          sed -i "s/Created with/© Malinkang |/" quartz/components/Footer.tsx 2>/dev/null || true

      - name: Build Quartz
        working-directory: /tmp/quartz
        run: npx quartz build

      - name: Deploy to quartz-deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: /tmp/quartz/public
          publish_branch: quartz
          force_orphan: true

  mkdocs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Prepare MkDocs project
        run: |
          mkdir -p /tmp/mkdocs/docs
          rsync -av --delete \
            "$GITHUB_WORKSPACE"/ \
            /tmp/mkdocs/docs/ \
            --exclude '.git' \
            --exclude '.github' \
            --exclude '.obsidian'
          if [ ! -f /tmp/mkdocs/docs/index.md ]; then
            echo "# Notes Index" > /tmp/mkdocs/docs/index.md
          fi
          echo "site_name: Malinkang's Notes" > /tmp/mkdocs/mkdocs.yml
          echo "docs_dir: docs" >> /tmp/mkdocs/mkdocs.yml
          echo "theme:" >> /tmp/mkdocs/mkdocs.yml
          echo "  name: material" >> /tmp/mkdocs/mkdocs.yml

      - name: Install MkDocs
        run: pip install mkdocs mkdocs-material

      - name: Build MkDocs
        working-directory: /tmp/mkdocs
        run: mkdocs build --clean

      - name: Deploy to mkdocs-deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: /tmp/mkdocs/site
          publish_branch: mkdocs
          force_orphan: true
